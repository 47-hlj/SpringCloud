# bootstrap.yml
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka

spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      name: user-service
      profile: dev
